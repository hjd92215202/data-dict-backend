# 架构评估与高并发/高可用规划（data-dict-backend）

## 1. 当前架构摘要

- 技术栈：Axum + Tokio 提供 API 服务，PostgreSQL 承担事务数据，Qdrant 承担向量检索，FastEmbed 离线模型负责向量化。
- 架构分层：Handler（接口层）→ Service（业务层）→ Model（数据访问映射），并使用中间件做 JWT + 角色校验。
- 搜索策略：先 SQL 模糊/同义词匹配，未命中再走向量召回（混合检索）。
- 任务场景：用户提交字段申请，管理员在任务列表处理。

## 2. 当前适合的业务规模与边界

### 2.1 更适合的规模

- 中小规模数据标准管理平台（百万级以下字段/词根可平稳运行，取决于向量维度与节点规格）。
- 以内网部署、知识库更新频率中等、检索 QPS 中低为主的业务。

### 2.2 已有优点

- DB 与向量库职责清晰，便于演进。
- 启动时做词典加载与向量冷启动同步，降低首次查询冷延迟。
- 已考虑 `pg_trgm` + GIN、Qdrant `on_disk_payload` 等性能点。

### 2.3 核心瓶颈（高并发下）

- 单实例内嵌入模型通过全局 `Mutex<TextEmbedding>` 串行化调用，向量化路径会形成热点锁。
- 启动流程包含全量同步到 Qdrant，数据规模增长后会明显拉长启动时间和恢复时间。
- CORS 全开放、JWT 密钥硬编码，安全与多环境治理能力不足。
- 默认管理员账户自动注入与固定弱口令风险较高，不利于生产合规。

## 3. 高并发优化建议（从立刻可做开始）

### P0（1-2 周，低风险）

1. **嵌入推理解耦**
   - 把向量化从 API 进程拆为独立“Embedding Worker”服务（可多副本）。
   - API 仅发请求并设置超时与熔断；支持批量化（micro-batch）降低单位成本。
2. **读写路径分离**
   - 创建/更新词根和字段仅写 DB + 发送异步事件；Qdrant 更新改为异步消费，避免写接口受向量库抖动影响。
3. **连接池与限流**
   - 为不同路由分组配置不同并发阈值（public 搜索、admin 导入、auth 登录）；避免导入流量拖垮在线查询。
   - PgPool 按 CPU 与 DB 规格调参并压测验证，不建议仅固定 20。
4. **SQL 优化**
   - 给高频过滤条件补齐索引（如 `notification_tasks(is_read, created_at)`、`standard_fields(created_at)`）。
   - 对 `ILIKE '%q%'` 统一配合 trigram 索引策略，避免全表扫描。

### P1（1-2 个月，中风险）

1. **缓存层引入**
   - 对热门搜索和建议接口增加 Redis 缓存（短 TTL + 主动失效）。
   - 对词根词典做版本化缓存，减少频繁 DB 读取。
2. **异步任务系统**
   - 引入消息队列（RabbitMQ/Kafka/NATS），把“向量生成+Qdrant upsert”作为可重试任务。
   - 失败入 DLQ（死信队列），支持补偿任务。
3. **查询融合排序**
   - SQL 召回与向量召回做分层融合（Reciprocal Rank Fusion），提升语义和精确命中平衡。

### P2（季度级，架构升级）

1. **多租户与隔离**
   - 按租户/业务域隔离 collection 或 payload filter，支持租户级限流与配额。
2. **水平扩展**
   - API 无状态化 + K8s HPA 按 CPU/QPS 自动扩容。
   - Qdrant 升级集群模式（副本 + 分片）。
3. **写路径一致性模型**
   - 建立“DB 为源、向量库最终一致”的事件溯源机制（outbox pattern）。

## 4. 高可用与容灾建议

## 4.1 服务层（API）

- 至少 2 副本 + 反向代理/LB 健康检查。
- 健康检查拆分为 `liveness`（进程活性）与 `readiness`（DB/Qdrant/Embedding 可达）。
- 增加超时、重试、熔断与舱壁隔离：防止下游雪崩。

## 4.2 数据层（PostgreSQL）

- 主从复制 + 自动故障转移（如 Patroni）。
- 定期 PITR 备份演练（不是只做备份）。
- 关键表建立归档策略与分区（日志类任务表可按时间分区）。

## 4.3 向量层（Qdrant）

- 生产建议集群模式（副本 + shard），并开启 snapshot 定时备份。
- 将导入与在线检索资源隔离（独立队列、限速）。

## 4.4 配置与安全

- JWT secret、默认账号策略必须外置到密钥管理系统（Vault/KMS/环境变量注入）。
- 管理端接口增加审计日志（谁在何时改了哪些标准字段/词根）。

## 5. 面向业务场景的演进规划

### 场景 A：数据治理平台（管理员主导）

- 核心诉求：准确性、可追溯、变更审批。
- 建议：引入字段版本、审批流、回滚能力；向量仅作建议，不直接覆盖标准结果。

### 场景 B：智能检索服务（用户查询高峰）

- 核心诉求：低延迟、高并发。
- 建议：建立热词缓存、预计算向量、向量召回阈值动态调参；压测目标 P95 < 150ms（SQL 命中）/ < 300ms（向量回退）。

### 场景 C：批量导入与模型迭代（离线任务重）

- 核心诉求：导入吞吐与在线稳定并存。
- 建议：离线导入窗口 + 异步索引重建 + 导入限速；生产切换采用双写或灰度 collection。

## 6. 推荐 KPI/SLO（可直接落地）

- 可用性：API 月度可用性 ≥ 99.9%。
- 延迟：
  - `/api/public/search`：P95 < 200ms，P99 < 400ms。
  - `/api/public/similar-roots`：P95 < 300ms。
- 正确性：向量更新任务成功率 ≥ 99.99%，失败可在 10 分钟内自动补偿。
- 数据一致性：DB→Qdrant 同步延迟 P95 < 5s。

## 7. 实施路线图（建议）

1. **第 1 阶段（2 周）**：安全加固（密钥外置、默认管理员策略下线）、限流与超时、索引补全。
2. **第 2 阶段（4 周）**：引入队列与 outbox，向量写入异步化，完善监控告警。
3. **第 3 阶段（4-8 周）**：Embedding 服务化 + Redis 缓存 + API 水平扩展。
4. **第 4 阶段（季度）**：Qdrant 集群化、跨可用区容灾、演练自动故障切换。

## 8. 总结

当前项目已经具备“结构清晰、功能完整”的基础形态，适合中等规模的标准字典场景。若目标升级到高并发与高可用，关键在于：

- 把**向量化与索引更新从同步链路剥离**；
- 把**单点依赖（单实例 API/单机 Qdrant/硬编码密钥）治理为可扩展、可恢复架构**；
- 用**SLO 驱动**性能、稳定性与业务体验的持续优化。
